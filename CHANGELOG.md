# Changelog

## Unreleased

### Fixed

- Fixed takeoff clicks after condition sidebar reloads so the clicked takeoff's condition is reselected and highlighted when the previous highlight was cleared.
- Fixed OSP exports so the embedded OST references drawing files under the local OST working folder instead of package-internal `TempImages!.tmp` paths.
